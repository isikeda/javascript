//CODING CHALLENGE #1
/*let massMark = 78; // in kg
let massJohn = 92;
let heightMark = 1.69; // in meter
let heightJohn = 1.95;

let BMIMark = massMark / heightMark ** 2;
let BMIJohn = massJohn / heightJohn ** 2;

let markHeigherBMI = BMIMark > BMIJohn;

console.log(BMIMark, BMIJohn, markHeigherBMI);
*/

//CODING CHALLENGE #2
/*
if(markHeigherBMI){
    console.log("Mark's BMI is higher than John's!");
}else{
    console.log("John's BMI is higher than Mark's!");
}
*/

/*
if (markHeigherBMI) {
    console.log(`Mark's BMI (${BMIMark}) is higher than John's (${BMIJohn})!`);
} else {
    console.log(`John's BMI (${BMIJohn}) is higher than Mark's (${BMIMark})!`);
}
*/

//CODING CHALLENGE #3
/*
const dolp = (97 + 112 + 101) / 3;
const koala = (109 + 95 + 106) / 3;

console.log(dolp, koala);

if (dolp > koala) console.log("Winner is dolphins!");
else if (koala > dolp) console.log("Winner is koalas!");
else console.log("There is a draw!");

if (dolp > koala && dolp >= 100) console.log("Winner is dolphins!");
else if (koala > dolp && koala >= 100) console.log("Winner is koalas!");
else console.log("There is no winner!");
*/

//CODING CHALLENGE #4
/*
const bill = 430;
let tip = (bill >= 50 && bill <= 300) ? (bill * 15 / 100) : (bill * 20 / 100);

console.log(`The bill was ${bill}, the tip was ${tip}, and the total value ${tip + bill}`);
*/
